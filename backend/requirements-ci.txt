# Облегчённые зависимости для CI — без тяжёлых OCR/ML/GPU пакетов.
# paddleocr, paddlepaddle, paddlepaddle_gpu импортируются лениво (внутри функций),
# поэтому Django запускается и тесты проходят без них.

# Core
django==4.2.16
djangorestframework==3.15.2
drf-spectacular==0.27.2
django-cors-headers==4.4.0
python-dotenv==1.0.1
requests==2.32.3
Pillow==10.4.0

# Parsing
beautifulsoup4==4.12.3
lxml==5.3.0

# AI stack (только langchain — GigaChat вызывается лениво, без OCR)
langchain==1.2.15
langchain_gigachat==0.5.0

# TG bot
pyTelegramBotAPI==4.33.0

# Document parsing (без paddleocr/paddlepaddle)
pdf2image==1.17.0
python-docx==1.2.0
